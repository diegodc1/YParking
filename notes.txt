
Tem que fazer:
--Colocar os tooltips nos botões do sidebar. ✔️

--arrumar alinhamento sidebar

--algumas paginas o tooltip n está funcionando

--emitir relatorios

##Talvez:
--O usuario poder escolher o layout do dashboard.


## Notas 29/11
-- Colocar data e horário nos movimentos
-- Não Excluir, e sim desativar
-- Colocar no banco de dados quem excluiu
-- Mudar a "titulariedade" do veículo, ou seja, mudar o dono.
--colocar/criar mais niveis de acesso ao sistema
-- se o user tiver desativado não pode fazer login.
-- esqueceu senha
-- edição seções
-- log-out
-- arrumar bug modal não abre para reativar veicula, pagina lista de veiculos
--google sharts* para grafico


--Tem que ter na entrega:
--explicar como é feito o cálculo das proximas saídas de veículos.


//comando sql de proximo veículos a sair:
SELECT ckout_client_id, avg(ckout_time) FROM checkout WHERE ckout_client_id = 84 AND ckout_status = 'Finalizado' AND ckout_date::date >= CURRENT_DATE - INTERVAL '4 days' GROUP BY ckout_client_id



